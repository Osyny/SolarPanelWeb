name: Azure Static Web Apps CI/CD
env:
  NPM_CONFIG_LEGACY_PEER_DEPS: true

on:
  push:
    branches:
      - dev
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - dev

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
        env:
          NPM_CONFIG_LEGACY_PEER_DEPS: true
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_AMBITIOUS_SMOKE_0CCA1EA03 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "solar-panels-web" # App source code path
          api_location: "" # Api source code path - optional
          output_location: "dist" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######
        env:
          NPM_CONFIG_LEGACY_PEER_DEPS: true

  print_dist_dir_content:
    runs-on: ubuntu-latest
    name: Print ./dist dir content
    steps:
      - name: Run script
      - run: ls dist/solar-panels-web
      # - script: |
      #     echo "******* Root:"
      #     ls /
      #     echo "******* Current dir:"
      #     ls .
      #     echo "******* ./dist/ content:"
      #     ls dist
      #     echo "******* ./dist/solar-panels-web content:"
      #     ls dist/solar-panels-web
      #     echo "******* ./dist/solar-panels-web/browser content:"
      #     ls dist/solar-panels-web/browser
      #   displayName: "!!!!!!! RUN SCRIPT !!!!!!"

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_AMBITIOUS_SMOKE_0CCA1EA03 }}
          action: "close"
